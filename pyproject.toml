[project]
name = "postcode-migrator"
version = "0.1.0"
description = "CSV to PostgreSQL Postcode Migrator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.2",
    "psycopg[binary]>=3.3.3",
    "sqlalchemy>=2.0.49",
    "sqlmodel>=0.0.38",
]
